<?xml version="1.0" encoding="utf-8"?>
<!-- res/xml/ussd_service_config.xml -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/ussd_service_desc" />
    <!--
      packageNames: intentionally omitted so the service can DISCOVER which package renders
      USSD dialogs on this phone (log event.packageName once — spike step 1). After discovery,
      set android:packageNames="com.android.phone,<oem.dialer.pkg>" to scope the service tightly.
    -->
